# Ferramentas

SEFAZ: Certidão Negativa de Débitos Estaduais expõe 1 ferramenta (todas somente leitura).

### 1. `sefaz_certidao_debitos_consultar`
**Input**: `cnpj` (opcional), `cpf` (opcional), `ie` (opcional), `cep` (opcional), `cpf_emissao` (opcional), `uf`, `preferencia_emissao` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

SEFAZ: Certidão Negativa de Débitos Estaduais, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ: Certidão Negativa de Débitos Estaduais
```
